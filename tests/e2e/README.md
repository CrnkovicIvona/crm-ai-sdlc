# End-to-end tests

Reserved for Playwright tests when the application is runnable.

Do not add placeholder tests that pass without a real user journey.
Target Vercel Preview/`test` when that environment exists.
