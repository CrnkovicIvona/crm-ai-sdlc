# Exploratory session: EXP-2026-09-16

- Session: EXP-2026-09-16-2 (supersedes 16-1 unauth-only pass)
- Date: 2026-09-16
- Tester / agent: Cursor Agent (`exploratory-qa-expert`)
- Environment: Production
- URL: https://crm-ai-sdlc.vercel.app/
- Role(s): unauthenticated, then **VIEWER** (read-only). ADMIN not
  used.
- Charter: Live Production at the canonical origin. Unauth login and
  protected URLs; VIEWER CRM read + dashboard observation; mandatory
  messy paths that do not write production data.
- Oracle: `docs/specifications/functional/AUTH-001.md`,
  `docs/specifications/technical/AUTH-001.md`,
  `docs/features/CRM-001/` (VIEWER cannot CREATE; search match
  **C008 BLOCKED**), `docs/features/DASH-001/` frozen KPIs (`N/A`
  when opening base is 0).
- Risk focus: Authn redirect, credential exposure on login, VIEWER
  write boundary, session after logout
- Browser tool: **computer-use fallback** (Chrome). Playwright MCP
  `browser_*` **not connected**. Not claimed as Playwright MCP.
- Product code modified: **No**
- Production data modified: **No**

## Charter

```text
Explore Production https://crm-ai-sdlc.vercel.app/
Roles: unauthenticated, VIEWER (on-page demo aside; E2E_* unset).
Do not log in as ADMIN. Do not create/edit/delete clients.
Mandatory messy paths: empty/invalid login, one failed sign-in,
direct /app URLs, Back/Forward, refresh, ~400px, second tab,
VIEWER /app/clients/new, logout then protected URL.
Do not print secret values. Do not claim TC-### PASSED.
```

## Coverage

| Area           | Role            | Status             | Notes                                                 |
| -------------- | --------------- | ------------------ | ----------------------------------------------------- |
| Authentication | Unauthenticated | Partially explored | Login, HTML5, one generic fail, redirects, logout     |
| Authentication | VIEWER          | Partially explored | Login via on-page aside; landing `/app/clients`       |
| CRM            | VIEWER          | Partially explored | List, one detail (read-only UI); create URL blocked   |
| CRM            | ADMIN           | Not explored       | Production write boundary; ADMIN not used             |
| Dashboard      | VIEWER          | Partially explored | Last 30 days KPIs observed; period buttons not cycled |
| Dashboard      | ADMIN           | Not explored       | Same                                                  |
| CRM search     | VIEWER          | Not explored       | C008 matching rule **BLOCKED**                        |

## Happy path

Exploratory observation only — **not** `TC-### PASSED`.

- `/` redirected to `/login`. Form: heading BankCRM login, email,
  password, Log in.
- Failed sign-in with a non-seed email (`nobody-exp-qa@example.invalid`)
  showed in-page `Authentication failed.` (matches AUTH generic
  failure; no account-exists wording).
- VIEWER session landed on `/app/clients`. Header showed VIEWER,
  Read-only access, Log out.
- Dashboard at `/app/dashboard` showed KPI cards and charts (Last 30
  days selected).
- VIEWER client detail showed Identity + Products; no edit/delete
  controls in the captured UI.
- Log out returned to `/login`. Subsequent `/app/dashboard` redirected
  to `/login`.

## Messy paths

| Messy path                             | Status    | Result / reason if not tried                                                                                   |
| -------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| Interrupt form + Back                  | Tried     | Unauth: stayed on login; no CRM shell                                                                          |
| Refresh after Save                     | Not tried | No authenticated write this session (production read-only)                                                     |
| Double Save / submit                   | Tried     | Unauth: double-click Log in after failed sign-in; still generic error, no CRM. Authenticated Save not tried    |
| Browser Back / Forward                 | Tried     | Unauth after protected redirect: stayed on `/login`. VIEWER: dashboard ↔ clients navigation kept session       |
| Dirty / stale / extra tab              | Tried     | Second tab unauth `/app/dashboard` → `/login`                                                                  |
| Partial / Cancel                       | Tried     | Empty submit and invalid email: native HTML5 tooltips. Delete dialog not opened (no VIEWER delete control)     |
| Role leftover (logout then other role) | Tried     | Logout then `/app/dashboard` → login. ADMIN not used afterward                                                 |
| Empty / large / dashboard period       | Partially | Last 30 days shown; Last 7 / 90 / This year / Custom **not** clicked. List had many rows (PII not copied here) |
| Direct URL `/app/dashboard`            | Tried     | Unauth → `/login`. VIEWER → dashboard KPIs                                                                     |
| Direct URL `/app/clients`              | Tried     | Unauth → `/login`. VIEWER → list                                                                               |
| Direct URL `/app/clients/new`          | Tried     | Unauth → `/login`. VIEWER → redirected to `/app/clients` (matches CRM TC-C009 steps as observation only)       |
| Direct URL `/app/clients/:id`          | Tried     | Unauth dummy UUID → `/login`. VIEWER opened an existing row (read-only)                                        |
| Direct URL `/access-denied`            | Tried     | Unauth → `/login` (AccessDeniedPage navigates guests to login; not treated as BUG)                             |
| Refresh mid-dashboard                  | Tried     | VIEWER F5 on dashboard: still authenticated; KPIs still shown                                                  |
| ~400px viewport                        | Tried     | Login form usable; no horizontal scroll reported                                                               |

## Findings

### BUG

None confirmed against written AUTH / CRM / DASH oracles for this
session.

Churn Rate `N/A` with Active Clients 525, New Clients 525, Churned 0
on Last 30 days is **consistent** with DASH-001 when opening active
base is 0 (AC-D007 / FS edge: opening 0 → `N/A`, not 0%). Not filed
as a defect.

Do not treat the login demo-user aside as a confirmed AC violation:
AUTH FS/TS do not require it and do not forbid a portfolio hint.

### IMP

- HTML5-only required/email messages vs in-page `login-error` after
  failed `signIn`. Inconsistent placement. Not a BUG without an AC
  that forbids native `required`.
- Login `test-users` aside on Production (plaintext demo credentials
  in the document). Production hygiene / portfolio question. See
  QUESTION. Not promoted to `BUG-004`.

### QUESTION

`TBD — HUMAN DECISION REQUIRED`

Should Production omit the login-page seed-user aside? The aside is in
`src/pages/LoginPage.tsx` and claims the specification requires two
test users; AUTH-001 FS/TS do not describe that UI. Values are not
repeated in this report.

Forgot-password / remember-me are out of AUTH-001 scope (not a bug).

### OBSERVATION

- Login aside present (ADMIN and VIEWER headings). Credentials not
  copied here.
- VIEWER has no visible create/edit/delete controls on the explored
  list/detail surfaces; `/app/clients/new` redirected to the list.
- Dashboard Last 30 days (VIEWER): Active 525, New 525, Churned 0,
  Churn Rate N/A, Net +525, Product Adoption Rate 19.8%, Clients by
  Product showed Bank account 101 / 19.2%. Formulas not independently
  recomputed from SQL.
- Console: computer-use reported no issues on the login page.
- `E2E_ADMIN_*` / `E2E_VIEWER_*` were **unset** in the agent
  environment; VIEWER login used the on-page aside (values not
  printed).

## Evidence

- Canonical origin `https://crm-ai-sdlc.vercel.app/` (not a unique
  `*.vercel.app` preview alias).
- computer-use screenshots captured locally under `/tmp/computer-use/`;
  **not** committed (login aside and client list would leak demo
  passwords or client PII into git).
- Walkthrough screenshot of VIEWER dashboard only (no password aside,
  no client PII): agent artifact `exp_viewer_dashboard_production.webp`.

## Confidence

- High: unauthenticated protected `/app/*` redirects to login; VIEWER
  cannot open create form; logout clears CRM access; generic failed
  login copy `Authentication failed.`
- Medium: dashboard KPI correctness (UI observed; opening-base
  arithmetic inferred, not proven from DB).
- Low: ADMIN write paths, custom date range, search matching (C008
  BLOCKED), RLS isolation vs another role — **not explored**.

## Areas not explored

ADMIN CRUD, delete confirmation, double-Save of a client, dashboard
period/custom range, C008 search, VIEWER vs ADMIN data isolation in
the same dataset, Playwright MCP console/network APIs.

## Blockers

- Playwright MCP `browser_*` unavailable; used computer-use.
- No `E2E_*` secrets in this environment (names only).
- Production write and ADMIN sessions out of charter.

## Follow-up charters

1. Same URL with Playwright MCP + `E2E_*`: ADMIN on a **named
   disposable** client only if PO authorizes writes; dashboard period
   buttons; role leftover ADMIN → logout → VIEWER.
2. PO decision on login `test-users` aside on Production.

## Bugs promoted

| Session id | `docs/bugs/`                               |
| ---------- | ------------------------------------------ |
| —          | none (QUESTION on seed aside, not BUG-004) |
