# Exploratory QA Report — 2026-09-16

## Context

- Session: EXP-2026-09-16-3 (aligned charter)
- Date/time: 2026-09-16 ~17:13 UTC
- Tester/agent: Cursor Agent (`exploratory-qa-expert`)
- Source branch: `cursor/exploratory-qa-skill-73b9` (`9546543`)
- Environment: Production
- Live URL: https://crm-ai-sdlc.vercel.app/
- Backend: Production Supabase
- Database: Production database
- Browser tool: Playwright Chromium headless (one-off local driver;
  **not** MCP, **not** `npm run test:e2e`)
- Playwright MCP: unavailable (computer-use also blocked: prior
  session hit the 100-image cap)
- Scope: unauthenticated, VIEWER, ADMIN; safe session-client CRUD
- Product code modified: **No**

Live SPA is Production (`main` deploy). This branch’s commits are
docs/skill only. Do not claim Production contains the skill branch
`src/` (there is no `src/` change on this branch).

Identities (no passwords):

- VIEWER: authenticated using the application's documented test-user
  mechanism.
- ADMIN: authenticated using the application's documented test-user
  mechanism.

## Charter

```text
Compare with EXP-2026-09-16-2 using the aligned skill: Production
URL, login-page test users, ADMIN safe writes, messy paths, C008
BLOCKED. Delete only the session-created client.
```

## Oracle

- AUTH: FS + TS AUTH-001 (generic failure; unauth = login surface)
- CRM: `docs/features/CRM-001/` (C008 BLOCKED)
- DASH: `docs/features/DASH-001/` (`N/A` when opening base is 0)
- Risk/lifecycle: exploratory investigation; human QA gate remains

## Coverage

| Area             | Unauthenticated | VIEWER   | ADMIN    | Status / notes                                        |
| ---------------- | --------------- | -------- | -------- | ----------------------------------------------------- |
| Authentication   | Explored        | Explored | Explored | Generic fail; both roles via login aside              |
| Authorization    | Explored        | Explored | Explored | Direct `/clients/new`; leftover after ADMIN logout    |
| CRM list         | N/A             | Explored | Explored | VIEWER 20 rows on first page                          |
| CRM detail       | N/A             | Explored | Explored | VIEWER: no Save/Delete                                |
| CRM create       | N/A             | Explored | Explored | VIEWER redirected to list; ADMIN created then deleted |
| CRM edit         | N/A             | N/A      | Partial  | Save after create; hydrate needs a short wait         |
| CRM delete       | N/A             | N/A      | Explored | Session client only; dialog Cancel then Confirm       |
| Dashboard        | N/A             | Explored | Explored | Last 30 default; VIEWER also Last 7                   |
| Session / logout | Explored        | Explored | Explored | Protected URL after logout → login                    |
| Messy paths      | Explored        | Explored | Explored | See table                                             |

C008 search: Blocked — requirement remains BLOCKED (search box not
judged).

## Comparison with session 16-2

| Topic                     | 16-2 (old skill)                 | 16-3 (this run)                         |
| ------------------------- | -------------------------------- | --------------------------------------- |
| Roles                     | Unauth + VIEWER                  | Unauth + VIEWER + ADMIN                 |
| Identities                | Treated `E2E_*` unset as blocker | Login-page `test-users` used            |
| Production writes         | None (read-only rule)            | Create / edit / soft-delete session row |
| `test-users` aside        | QUESTION / hygiene IMP           | Intended; not a BUG                     |
| Browser                   | computer-use                     | Playwright CLI fallback                 |
| VIEWER `/app/clients/new` | Redirect to list                 | Redirect to list                        |
| Failed login copy         | `Authentication failed.`         | Same                                    |
| VIEWER Last 30 Active     | 525                              | 561                                     |
| ADMIN CRUD                | Not explored                     | Explored (disposable client)            |

## Findings

Not `TC-### PASSED`.

### BUG

None confirmed.

Empty ADMIN fields immediately after reload were a **harness race**:
a follow-up wait showed first name `Hydrate` on a second disposable
create that was then deleted. Not filed as a product defect.

### IMP

- Login empty/invalid still uses native HTML5 tooltips; failed
  `signIn` uses in-page `Authentication failed.` Same pattern as 16-2.
  No AC requires one style.

### QUESTION

`TBD — HUMAN DECISION REQUIRED`

- No Cancel control on the ADMIN client form (only Back to clients).
  FS does not require a Cancel button.
- Dirty phone value still present after Dashboard then browser Back
  (`0999999999` in this run). Persistence of unsaved form state is
  unspecified.
- AUTH FS/TS still do not describe the login `test-users` aside (spec
  silence only). Human has stated the aside is intentional for this
  practice app; not treated as a defect.

### OBSERVATION

- Login aside present; credentials not copied here.
- VIEWER: no New client control; `/app/clients/new` → `/app/clients`.
- VIEWER detail: Save/Delete absent.
- ADMIN: empty create shows the five field errors from
  `clientValidation`. Whitespace-only first name → “Enter a first
  name.”
- ADMIN create: in-page `Client created.` then detail URL. Soft-delete
  of that row: `Client deleted.` and email no longer on the list.
- VIEWER Last 30: Active 561, New 561, Churned 0, Churn Rate N/A, Net
  +561, Adoption 19.8%. Last 7: New 509, Churn Rate 0%, Net +509.
  `N/A` vs `0%` matches opening-base 0 vs non-zero (DASH-001).
- ADMIN Last 30 (after create, before delete): Active 562, New 738,
  Churned 176, Churn Rate N/A, Net +562. Net = New − Churned. Do not
  compare VIEWER 561 vs ADMIN 562 as RLS: they were sampled at
  different times (extra session row).
- ~400px login: no likely horizontal overflow.

## Messy Paths

| Path            | Tried | Result                                                                 | Finding       |
| --------------- | ----- | ---------------------------------------------------------------------- | ------------- |
| Refresh         | Yes   | Login reload stays login. After create, detail reload hydrates (wait). | OBSERVATION   |
| Double Save     | Yes   | Second Save on edit; no extra client created in this pass              | OBSERVATION   |
| Back/Forward    | Yes   | Unauth protected URL then Back/Forward stay on login                   | OBSERVATION   |
| Dirty state     | Yes   | Unsaved phone still there after Dashboard + Back                       | QUESTION      |
| Cancel          | Yes   | No form Cancel. Delete dialog Cancel left the form open                | QUESTION / OK |
| Partial input   | Yes   | HTML5 login; ADMIN field errors as documented in validation copy       | OBSERVATION   |
| Role transition | Yes   | ADMIN logout then VIEWER: `/clients/new` → list, no create button      | OBSERVATION   |
| Empty state     | No    | Not tried — list had rows; empty-list UI not forced                    | —             |
| Large input     | No    | Not tried — avoid huge strings in production                           | —             |
| Direct URL      | Yes   | Unauth `/app/*` → `/login`. VIEWER `/new` → list. ADMIN `/new` = form  | OBSERVATION   |

## Not Explored / Blocked

- **Blocked:** C008 search-match semantics.
- **Not explored:** Custom dashboard range; delete of non-session
  clients; Playwright MCP console/network APIs; empty-list UI.

## Evidence

- Driver log: `/tmp/exp-aligned-log.json` (not committed).
- Session client UUID created then deleted:
  `22362a22-db66-41ca-98e6-02c44a013bc1`.
- Hydrate check (second disposable row, then deleted): immediate
  empty input, then first name present after wait.
- Screenshots not committed (login aside / PII risk). Walkthrough:
  VIEWER and ADMIN dashboard KPI pages.

## Confidence

- High: unauth redirects; VIEWER cannot open create form; ADMIN
  create+delete of session client; generic login failure copy.
- Medium: edit persistence (hydrate wait required in automation).
- Low: C008; custom period; RLS isolation ADMIN vs VIEWER on the
  same timestamp (not compared).

## Follow-Up Recommendations

- Re-run with Playwright MCP when connected (console/network).
- Optional automation later: VIEWER `/clients/new` redirect; ADMIN
  create then refresh after waiting for field hydrate.
- Revisit C008 when unblocked.

## Bugs promoted

| Session id | `docs/bugs/` |
| ---------- | ------------ |
| —          | none         |

## Human QA Gate

Final approval: HUMAN QA REQUIRED

Human QA gate remains required.

## Prior session 16-2 (old charter)

Unauth + VIEWER only; ADMIN omitted; `E2E_*` treated as blocker;
production treated as read-only. See git `93e32d9` /
`docs/test-reports/EXP-2026-09-16.md` history before this revision.
