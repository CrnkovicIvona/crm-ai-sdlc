# Exploratory session: EXP-2026-09-16

- Session: EXP-2026-09-16-1
- Date: 2026-09-16
- Tester / agent: Cursor Agent (`exploratory-qa-expert`)
- Environment: Production
- URL: https://crm-ai-sdlc.vercel.app/
- Role(s): **unauthenticated only**
- Charter: Live production unauthenticated surface (login, protected
  URLs, messy Back/Forward). ADMIN/VIEWER in-app flows out of scope
  this run (`E2E_*` unset in the agent environment).
- Oracle: AUTH-001 FS/TS, skill §7–16
- Risk focus: Authn redirect, credential exposure, protected routes
- Browser tool: **computer-use fallback** (Chrome). Playwright MCP
  `browser_*` **not connected**. Not claimed as Playwright MCP.
- Product code modified: **No**

## Charter

```text
Explore unauthenticated Production at https://crm-ai-sdlc.vercel.app/
Oracle: docs/specifications/functional/AUTH-001.md and technical spec.
Mandatory messy paths: direct /app URLs, Back/Forward, empty/invalid
login fields. Do not write client data. Do not print secret values.
```

## Coverage

| Area           | Role            | Status             | Notes                                      |
| -------------- | --------------- | ------------------ | ------------------------------------------ |
| Authentication | Unauthenticated | Partially explored | Login page, HTML5 required, redirects      |
| CRM            | ADMIN / VIEWER  | Not explored       | No `E2E_*` in this environment             |
| Dashboard      | ADMIN / VIEWER  | Not explored       | Same; unauth `/app/dashboard` → login only |

## Happy path

- `https://crm-ai-sdlc.vercel.app/` redirected to `/login`.
- Login form present (`BankCRM login`, email, password, Log in).
- Direct `/login` loaded the SPA (contrast historical BUG-001 404).

Exploratory observation only — **not** `TC-### PASSED`.

## Messy paths

| Messy path                             | Status    | Result / reason if not tried                                  |
| -------------------------------------- | --------- | ------------------------------------------------------------- |
| Interrupt form + Back                  | Tried     | Stayed on login; no CRM shell                                 |
| Refresh after Save                     | Not tried | No authenticated save this session                            |
| Double Save / submit                   | Not tried | No logged-in write; production read-only                      |
| Browser Back / Forward                 | Tried     | After protected-URL redirect, Back/Forward stayed on `/login` |
| Dirty / stale / extra tab              | Not tried | Single-tab unauth only                                        |
| Partial / Cancel                       | Tried     | Empty submit and invalid email: browser HTML5 tooltips        |
| Role leftover (logout then other role) | Not tried | Did not log in                                                |
| Empty / large / dashboard period       | Not tried | Dashboard not opened authenticated                            |
| Direct URL `/app/dashboard`            | Tried     | Redirect to `/login`, no dashboard KPIs                       |
| Direct URL `/app/clients/new`          | Tried     | Redirect to `/login`, no create form                          |

## Findings

### BUG

None confirmed against written AUTH AC for this unauthenticated pass.
Protected-route redirect matched fail-closed access (oracle).

Do not treat “demo users on the login page” as a confirmed AC
violation: AUTH FS/TS do **not** require that aside, and also do not
forbid a portfolio demo hint. See QUESTION / OBSERVATION.

### IMP

- HTML5-only required/email messages (browser chrome) vs in-page
  `login-error` used after failed `signIn`. Inconsistent error
  placement if a human cares about one pattern. Not filed as BUG
  without an AC that forbids native `required`.

### QUESTION

`TBD — HUMAN DECISION REQUIRED`

Should Production omit the login-page seed-user aside (emails and
passwords in the document)? The aside is in `src/pages/LoginPage.tsx`
and claims the specification requires two test users; AUTH-001 FS/TS
do not describe that UI. Secrets must not be copied into this report.

### OBSERVATION

- Login page includes a `test-users` aside (visible on Production).
  Values are not repeated here.
- Console: computer-use reported no issues on the login page.
- ~400px width: form remained usable; no horizontal scroll reported.
- `E2E_ADMIN_*` / `E2E_VIEWER_*` were **unset** here, so CRM and
  dashboard authenticated messy paths were not run.

## Evidence

- Production origin opened (not a unique `*.vercel.app` alias).
- computer-use screenshots were captured locally; **not** committed
  (aside would leak seed passwords into git).
- Web fetch of `/` showed login headings (same surface).

## Confidence

- High: unauthenticated redirect to login; `/app/dashboard` and
  `/app/clients/new` do not show CRM without a session.
- Low: ADMIN/VIEWER product quality — **not explored**.

## Areas not explored

Authenticated CRM CRUD, dashboard metrics, VIEWER RLS, logout,
double-submit Save, delete dialog, search (C008 still BLOCKED).

## Blockers

- Playwright MCP `browser_*` unavailable; used computer-use.
- No `E2E_*` secrets in the agent environment (names only; values
  never printed).

## Follow-up charters

1. Same URL with Playwright MCP + `E2E_*` set: VIEWER then ADMIN;
   dashboard filters; CRM persist/reload; messy double-Save on a
   **named disposable** client only if PO authorizes.
2. PO decision on login `test-users` aside on Production.

## Bugs promoted

| Session id | `docs/bugs/`                               |
| ---------- | ------------------------------------------ |
| —          | none (QUESTION on seed aside, not BUG-004) |
