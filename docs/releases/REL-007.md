# REL-007: BUG-004 dashboard stamps + AUTH-B001 login spacing

- Status: **release PR prepared**. After human merge to `main`:
  **ON_MAIN — not RELEASED** until production smoke **PASSED**.
- Date: 2026-09-19
- Release identifier: REL-007
- Release title: Dashboard stamp parity and login test-user spacing
- Source: `test` (`ad191ad` Merge pull request #55)
- Target: `main`
- Issue: none
- Human QA / READY_FOR_RELEASE: chat 2026-09-19 (“55 je mergan u
  test, može onda release”). Agent does not merge `main`.
- Human merge to `main`: pending

Git branch: `cursor/rel-007-bug004-auth-73b9`. Docs id **REL-007**.
Canonical name would be `release/REL-007`; this is the same
`cursor/rel-…` alias pattern as REL-006.

AUTH-001 and CRM-001 stay **`RELEASED`**. DASH-001 stays **`ON_MAIN`**
after merge (already on `main` from REL-006). BUG-004 and AUTH-B001
become **`ON_MAIN`** after merge, **not** `RELEASED` until smoke.

## Changes

Included (already on `test`; this PR promotes them to `main`):

- BUG-004 / TD-D005: migration
  `20260919193000_client_lifecycle_stamps.sql`; dashboard loader
  reads `client_lifecycle_stamps` so VIEWER Last-30 KPIs match ADMIN
  without widening deleted-row PII SELECT
- TC-D015 PGlite unit + local PostgREST RLS job; hosted Playwright
  `TC-D015-ui` remains SKIPPED without `TC_D015_REQUIRE_VIEW=1`
  (SKIPPED ≠ PASSED)
- AUTH-B001: vertical gap between login ADMIN, VIEWER, and italic
  note (`tests/e2e/auth.spec.ts` TC-008 spacing)
- Docs already on `test`: BUG-004 RCA/plan, EXP sessions, QA parking
  lot (not product scope)

Excluded:

- Agent merge to `main`
- Claiming `RELEASED` before production smoke
- C008 search-match oracle
- Product-level churn
- Widening `clients` deleted-row SELECT to VIEWER

## Risk

**High** (dashboard timestamps under RLS; PII must stay off the
stamp view). apply-schema on Production must record/create
`client_lifecycle_stamps`. If the view is missing, KPI mismatch or
loader fallback is **FAIL** on smoke, not SKIPPED.

## Production readiness

- [x] `test` CI green on SHA `ad191ad`
      ([run 35468058847](https://github.com/CrnkovicIvona/crm-ai-sdlc/actions/runs/35468058847)
      — Prettier, gitleaks, ESLint, Vitest 39 PASSED, Vitest local
      RLS TC-D015 1 PASSED, Playwright 26 PASSED / 1 SKIPPED
      (`TC-D015-ui` without `TC_D015_REQUIRE_VIEW=1`; skip ≠ pass)).
      Commitlint SKIPPED on push to `test` (job only on PRs)
- [x] QA / READY_FOR_RELEASE: human release blessing 2026-09-19
- [x] Test report: [../test-reports/DASH-001.md](../test-reports/DASH-001.md)
      (REL-007 addendum)
- [x] No secrets in the tree
- [x] New migration `20260919193000`; Production apply is
      `production-smoke.yml` job `apply-schema` (`db push` after
      history repair). Human already created the view on hosted
      Preview; Production still needs apply from `main`
- [x] Rollback: human revert merge on `main` / Vercel rollback
- [x] Smoke files: `tests/smoke/rel-003.spec.ts`,
      `tests/smoke/rel-004.spec.ts`, `tests/smoke/rel-006.spec.ts`,
      `tests/smoke/rel-007.spec.ts`
- [x] This file
- [ ] Production smoke — **not executed** until after human merge
      (workflow apply-schema then `npm run test:smoke`). Apply
      success is not smoke PASSED.

## Test report

Non-prod: [DASH-001.md](../test-reports/DASH-001.md). Production
smoke result belongs in the table below after the workflow runs.

## Production smoke

- Command: `SMOKE_BASE_URL=https://crm-ai-sdlc.vercel.app npm run test:smoke`
- CI: `.github/workflows/production-smoke.yml`
- Secrets: `E2E_ADMIN_*`, `E2E_VIEWER_*`, `PRODUCTION_SUPABASE_DB_URL`
  (missing = FAIL)

| #     | Smoke test                                         | Result       | Evidence    |
| ----- | -------------------------------------------------- | ------------ | ----------- |
| 1–5   | REL-003 AUTH                                       | NOT EXECUTED | after merge |
| 6–7   | REL-004 CRM list                                   | NOT EXECUTED | after merge |
| 8–10  | REL-006 dashboard unauth / ADMIN / VIEWER          | NOT EXECUTED | after merge |
| 11–14 | REL-007 login spacing + Last-30 KPI ADMIN = VIEWER | NOT EXECUTED | after merge |

Overall: **not executed**. State after merge is **ON_MAIN — not
RELEASED.**

## Rollback

Human reverts the release merge on `main` and/or Vercel Production
rollback. Agent does not deploy or merge `main`.

## Residual risk

C008 match oracle still **BLOCKED**. Hosted Playwright `TC-D015-ui`
SKIPPED on `test` CI. Production smoke test 4 is the executable
ADMIN=VIEWER check after apply-schema.
