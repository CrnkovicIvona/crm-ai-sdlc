# REL-003: AUTH-001 login, roles, and fail-closed access

- Status: **`ON_MAIN` — not `RELEASED`.** Human merged PR #12 → `main`
  (`45c5b7f`, 2026-08-22). Production smoke is executable in
  `tests/smoke/rel-003.spec.ts`. PO removed former step 1b
  (`GET /login`). Defined suite is steps 1–5 below. Do not treat
  merge-to-main as `RELEASED` until this suite is on `main` and
  recorded PASSED there.
- Date: 2026-08-22
- Release identifier: REL-003
- Release title: AUTH-001 — Bank employee login (ADMIN / VIEWER)
- Source: `test` (`d810e3e` Merge pull request #11)
- Target: `main`
- Issue: [#5](https://github.com/CrnkovicIvona/crm-ai-sdlc/issues/5)
  (closed while git lifecycle is still `ON_MAIN`; Issue close ≠
  `RELEASED`)
- Human QA / READY_FOR_RELEASE: recorded in Cursor chat 2026-08-22
  (“Ready for release” / “Ready for release na #5”). Agent did not
  self-approve this gate.
- Human merge to `main`: **done** — PR
  [#12](https://github.com/CrnkovicIvona/crm-ai-sdlc/pull/12), merge
  commit `45c5b7f`

This **is** a product increment (login UI + Auth + profiles). It is
**not** CRM-001 (Clients) and **not** DASH-001.

Git branch name follows the cloud-agent prefix
(`cursor/rel-003-auth-001-73b9`). Release id in docs remains **REL-003**.

## Changes

Included (already on `test`; this PR promotes them to `main`):

- AUTH-001 Vite + React app (`src/`): login, logout, CRM shell,
  `/access-denied`, fail-closed without usable ADMIN/VIEWER profile
- Non-prod migration `supabase/migrations/20260822150000_profiles.sql`
  (human applies; **agent does not apply to production**)
- Playwright TC-001–TC-009 (no TC-010; unknown credentials = TC-007)
- SDLC hygiene, MD index fold, CRM-001 **specifications only**
  (not implemented)

Excluded:

- CRM-001 implementation
- Agent merge to `main`
- Agent apply of SQL to production
- Lockout / password policy / MFA

## Risk

**High** (authentication / authorization). Residual hosting:
direct `GET /login` can 404 without an SPA rewrite
([BUG-001](../bugs/BUG-001.md)). PO removed that check from REL-003
smoke; unauthenticated smoke is `/` only.

## Production readiness

- [x] `test` CI green after #11:
      [run 32591762999](https://github.com/CrnkovicIvona/crm-ai-sdlc/actions/runs/32591762999)
      — Prettier, secrets, ESLint, Vitest, Playwright **PASSED**.
      Conventional Commits **SKIPPED** on push (job is pull_request
      only). SKIPPED ≠ PASSED.
- [x] QA / READY_FOR_RELEASE: human statement in chat 2026-08-22
- [x] Test report: [../test-reports/AUTH-001.md](../test-reports/AUTH-001.md)
      (plus later CI on `test`; TC-010 withdrawn)
- [x] Security: no service role in client; generic auth errors; RLS
      SELECT own profile; High review per
      [../sdlc/security-review.md](../sdlc/security-review.md)
- [x] No secrets in the tree (gitleaks PASSED)
- [x] Migration exists; **not** applied to production by the agent
- [x] Env names: `.env.example` (`VITE_SUPABASE_URL`,
      `VITE_SUPABASE_ANON_KEY`); values in secret stores
- [x] Rollback: human revert merge on `main` / Vercel rollback
- [x] Smoke suite: `tests/smoke/rel-003.spec.ts` (status below)
- [x] This file

## Production smoke (executable)

Do not mark PASSED unless `tests/smoke/` actually ran against
Production.

- Suite: `tests/smoke/rel-003.spec.ts`
- Config: `playwright.smoke.config.ts` (`SMOKE_BASE_URL`, no Vite
  webServer)
- Command: `SMOKE_BASE_URL=https://crm-ai-sdlc.vercel.app npm run test:smoke`
- CI: `.github/workflows/production-smoke.yml` (`deployment_status` on
  Production, or **Actions → Production smoke → Run workflow**)
- Credentials: GitHub secrets `E2E_ADMIN_EMAIL` /
  `E2E_ADMIN_PASSWORD` / `E2E_VIEWER_EMAIL` /
  `E2E_VIEWER_PASSWORD` (documented test users). Missing secrets =
  FAIL, not SKIPPED. No CRM records are created or deleted.
- Local e2e (`tests/e2e/`, `npm run test:e2e`) is **not** this suite.

Unauthenticated check is `https://crm-ai-sdlc.vercel.app/` only.
PO removed former step 1b (`GET /login`).

| #   | Executable test                         | Maps to                |
| --- | --------------------------------------- | ---------------------- |
| 1   | unauthenticated `/` shows login only    | AC-010 / TC-008        |
| 2   | failed login → `Authentication failed.` | AC-009 / TC-007        |
| 3   | ADMIN login → shell; logout → login     | AC-001, AC-004, AC-006 |
| 4   | VIEWER login → shell, no write hint     | AC-005                 |
| 5   | bundle has no `service_role`            | security               |

### Execution record

Production URL: `https://crm-ai-sdlc.vercel.app`

| When       | How                                                                             | 1    | 2    | 3    | 4    | 5    | Overall  |
| ---------- | ------------------------------------------------------------------------------- | ---- | ---- | ---- | ---- | ---- | -------- |
| 2026-08-23 | `SMOKE_BASE_URL=https://crm-ai-sdlc.vercel.app npm run test:smoke` (1b removed) | PASS | PASS | PASS | PASS | PASS | **PASS** |

Former 1b (`GET /login` 404) is recorded in [BUG-001](../bugs/BUG-001.md)
and is **not** part of this suite.

Status: **executed — PASS** for the defined 5-step suite (this branch).
AUTH-001 stays **`ON_MAIN` — not `RELEASED`** until this suite and
result are on `main`.

## Rollback

Human: revert the merge commit on `main`, or Vercel rollback. Do not
leave a half-applied production migration without a human plan.

## Post-release

1. Human merged `cursor/rel-003-auth-001-73b9` → `main` (PR #12).
2. Agent later synced that release into `test`. README/roadmap had
   conflicted statuses. Under the canonical model this release is
   **`ON_MAIN`**, not `RELEASED`, because production smoke is not
   PASSED. Not a force-push.
3. Agent fast-forwarded `test` to `2a7e5aa` (2026-08-22). Deleted
   remote `cursor/rel-003-auth-001-73b9` and
   `cursor/rel-003-sync-test-73b9`.
4. Smoke: PO removed former `GET /login` step. Defined suite is
   unauthenticated `/` plus steps 2–5. AUTH-001 remains `ON_MAIN`
   until this suite and a PASSED run are on `main`.

`test` and `main` will **not** share one SHA: the release was squashed
for commitlint. Product trees should match except this sync record.

## Human approval

Hard gate. Agent must not merge this PR to `main`.
