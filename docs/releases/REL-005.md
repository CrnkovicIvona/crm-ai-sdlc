# REL-005: Promote `test` (smoke apply-schema, README, CRM test isolation)

- Status: **not merged.** After a human merges this PR to `main`, CRM-001
  stays **`ON_MAIN` — not `RELEASED`** until production smoke **PASSED**.
- Date: 2026-09-13
- Release identifier: REL-005
- Release title: Promote `test` — production apply-schema, landing README,
  CRM e2e/RLS isolation
- Source: `test` (`1056268` Merge pull request #34)
- Target: `main`
- Issue: none
- Human QA / READY_FOR_RELEASE: PO asked to cut this release
  (2026-09-13, “ajde onda release napravit”). Agent did not self-approve
  Gate 3 for a new product increment.
- Human merge to `main`: **not done** until a human merges this PR.

Git branch: `cursor/rel-005-promote-test-73b9`. Docs id **REL-005**.

AUTH-001 stays **`RELEASED`** (REL-003). CRM-001 stays **`ON_MAIN` —
not `RELEASED`** (REL-004 product on `main`; this train does not claim
smoke PASSED).

## Changes

Included (already on `test`; this PR promotes them to `main`):

- `.github/workflows/production-smoke.yml`: job `apply-schema`
  (`supabase db push` from `main`, secret `PRODUCTION_SUPABASE_DB_URL`)
  then job `smoke`
- Root `README.md` landing page + `.cursor/rules/readme.mdc`
- Agent heal: Prettier and Playwright/Vitest `expect` on an owned PR
  branch (format/harness/product, new SHA; no skip)
- CRM-001 Playwright: one TC per case; C011 field-by-field; schema
  missing FAIL; C008 match still **BLOCKED**
- RLS integration: C013–C016 isolated `it()`; Vitest integration
  timeout 20s
- `.env.example` name `PRODUCTION_SUPABASE_DB_URL`

Excluded:

- Agent merge to `main`
- DASH-001
- C008 search-match oracle
- Claiming `RELEASED` or production smoke PASSED

## Risk

**Medium** (workflow + tests + docs). Production apply uses a GitHub
secret; missing secret **FAIL**. Residual: CRM smoke still **not
executed** until this workflow runs on `main`.

## Production readiness

- [x] `test` CI green on PR #34 SHA `9b7594d`
      ([run 34749303260](https://github.com/CrnkovicIvona/crm-ai-sdlc/actions/runs/34749303260)
      — Prettier, commitlint, gitleaks, ESLint, Vitest, Playwright
      SUCCESS)
- [x] QA / READY_FOR_RELEASE: human asked for this release 2026-09-13
- [x] Test report: [../test-reports/CRM-001.md](../test-reports/CRM-001.md)
      (historical High pack). Isolation re-run: PR #34 CI above.
- [x] No secrets in the tree
- [x] Migrations already on `main` (REL-004). This release adds the
      **apply** job so they can be pushed when smoke runs.
- [x] Rollback: human revert merge on `main` / Vercel rollback
- [x] Smoke files: `tests/smoke/rel-003.spec.ts`,
      `tests/smoke/rel-004.spec.ts`
- [x] This file
- [ ] Production smoke **PASSED** — **not executed** (ON_MAIN)

## Test report

Isolation pack: GitHub Actions run 34749303260 (Playwright SUCCESS
after TC-C005 harness fix). SKIPPED ≠ PASSED. Production smoke:
**MISSING**.

## Production smoke

- Command: `SMOKE_BASE_URL=https://crm-ai-sdlc.vercel.app npm run test:smoke`
- CI: `.github/workflows/production-smoke.yml` (`apply-schema` then `smoke`)
- Secrets: `PRODUCTION_SUPABASE_DB_URL`, `E2E_ADMIN_*`, `E2E_VIEWER_*`
  (missing = FAIL). Optional repo variable `SMOKE_BASE_URL`.
- After **human** merge to `main`: Actions → Production smoke → Run
  workflow (branch `main`, `base_url` if the variable is empty).
- Apply success is **not** smoke PASSED.

| #   | Smoke test              | Result       | Evidence |
| --- | ----------------------- | ------------ | -------- |
| —   | REL-003 + REL-004 files | not executed | —        |

Overall: **not executed**.

## Rollback

Human reverts this merge on `main` and/or rolls back Vercel Production.
Do not delete `test`.
