# REL-002: Git workflow sync (`test` equals `main`)

- Status: Prepared; waiting for human approval to merge `release/REL-002` → `main`
- Date: 2026-08-21
- Release identifier: REL-002
- Release title: Git workflow — sync `test` with `main` after each release
- Source: `test` (`ec5cef0a3b1571843851d2975c54ebf73592edc6`) plus this release record
- Target branch: `main` (`fdf3a581bbade9553f667b8f050185f4ea687510`)
- Scope: documentation and Cursor rules only
- Human approver: (required — not yet recorded)

This is **not** a CRM application release.

## Release objective

Put the Git rules already on `test` onto `main`, so production docs match QA:

- `release/<rel-id>` cut from `test`
- human merges `release/<rel-id>` → `main`
- then the same branch is merged into `test` so **`test` matches `main`**

Also format `.cursor/skills/release-and-verify/SKILL.md` so Prettier passes (CI on `test` at `ec5cef0` **FAILED** Prettier).

## Release scope

### Included

- `.cursor/rules/git-workflow.mdc`
- `.cursor/skills/release-and-verify/SKILL.md` (content + Prettier)
- `docs/git/branching.md`
- This REL-002 record

### Excluded

- Business functionality
- Application source
- Application dependencies
- Database / Supabase
- Agent merge to `main`

## Related

- Follows REL-001 / ENG-001 foundation
- PR #3 already put REL-001 notes on `test`

## Validation evidence

**PASSED** | **FAILED** | **SKIPPED** | **NOT APPLICABLE**

No application was tested.

| Check                                 | Status                   | Evidence                                                                                                                        |
| ------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| Prettier (this release branch, local) | PASSED                   | `npm run format:check` after formatting the skill                                                                               |
| Prettier (push `test` `ec5cef0`)      | FAILED                   | [run 32512373872](https://github.com/CrnkovicIvona/crm-ai-sdlc/actions/runs/32512373872) — skill markdown; fixed on this branch |
| Secret scan (`test` `ec5cef0`)        | PASSED                   | same run                                                                                                                        |
| Detect application source             | PASSED                   | `src/` absent                                                                                                                   |
| Conventional Commits on push          | SKIPPED                  | job only on `pull_request`                                                                                                      |
| ESLint                                | NOT APPLICABLE / SKIPPED | no `src/`; not passed                                                                                                           |
| Vitest                                | NOT APPLICABLE / SKIPPED | no `src/`; not passed                                                                                                           |
| Playwright                            | NOT APPLICABLE / SKIPPED | no `src/`; not passed                                                                                                           |

CI for this release PR to `main` is not claimed until it finishes.

## Known limitations

- `test` and `main` currently differ. After human merge of this PR, the agent should merge `release/REL-002` into `test` (per the new rule) so they match.
- Branch protection still a human GitHub setting.
- No CRM product.

## Deployment expectations

Human merge to `main` may refresh Vercel Production with docs only. Agent does not merge or deploy.

## Rollback

Human: revert the merge on `main`, or Vercel rollback. No production data.

## Post-release

After human merge to `main`:

1. Agent merges `release/REL-002` → `test` (do not force on conflict)
2. If both merges succeeded, delete `release/REL-002`
3. Confirm still no `src/`
4. Smoke is **not executed** unless a human authorizes and it is actually run

## Human approval

Hard gate. Agent must not merge this PR to `main`.
