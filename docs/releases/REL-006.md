# REL-006: DASH-001 dashboard + BUG-002/BUG-003 chrome

- Status: **release PR prepared**. After human merge to `main`:
  **ON_MAIN — not RELEASED** until production smoke **PASSED**.
- Date: 2026-09-14
- Release identifier: REL-006
- Release title: CRM Dashboard (DASH-001) and success/nav chrome
- Source: `test` (`0bea7eb` Merge pull request #43)
- Target: `main`
- Issue: none
- Human QA / READY_FOR_RELEASE: PO 2026-09-14 (“ok za sad mi se sve
  sviđa što je napravljeno, imaš blagoslov za release”). Agent does
  not merge `main`.
- Human merge to `main`: pending

Git branch: `cursor/rel-006-dash-001-73b9`. Docs id **REL-006**.

AUTH-001 and CRM-001 stay **`RELEASED`**. DASH-001 in this PR is
**`ON_MAIN`** after merge, **not** `RELEASED` until smoke.

## Changes

Included (already on `test`; this PR promotes them to `main`):

- DASH-001: `/app/dashboard`, frozen metrics, timestamp-only fetch,
  SVG/CSS charts, unit TC-D003–D013, e2e TC-D001–D014
- BUG-002: separate Dashboard / Clients nav buttons
- BUG-003: green `Client saved.` banner (BD-T012 copy unchanged)
- Heal: Prettier after human conflict resolve on agent PRs; CI stays
  `format:check`
- Vitest integration timeout 45s (RLS Auth 504 latency)

Excluded:

- Agent merge to `main`
- Claiming `RELEASED` before production smoke
- New migrations (DASH-001 uses existing CRM-001 tables)
- C008 search-match oracle
- Product-level churn

## Risk

**High** (dashboard reads clients/products under RLS; PII columns
must not be in KPI queries). No new npm chart libraries. Anon client
only.

## Production readiness

- [x] `test` CI green on SHA `0bea7eb`
      ([run 34856651861](https://github.com/CrnkovicIvona/crm-ai-sdlc/actions/runs/34856651861)
      — Prettier, gitleaks, ESLint, Vitest 34 PASSED, Playwright 26
      PASSED). Commitlint SKIPPED on push to `test` (job only on PRs)
- [x] QA / READY_FOR_RELEASE: human release blessing 2026-09-14
- [x] Test report: [../test-reports/DASH-001.md](../test-reports/DASH-001.md)
- [x] No secrets in the tree
- [x] No new migrations; apply-schema still records CRM-001 versions
- [x] Rollback: human revert merge on `main` / Vercel rollback
- [x] Smoke files: `tests/smoke/rel-003.spec.ts`,
      `tests/smoke/rel-004.spec.ts`, `tests/smoke/rel-006.spec.ts`
- [x] This file
- [ ] Production smoke — **not executed** until after human merge
      (workflow apply-schema then `npm run test:smoke`). Apply
      success is not smoke PASSED.

## Test report

Non-prod: [DASH-001.md](../test-reports/DASH-001.md). Production
smoke result belongs in the table below after the workflow runs.

## Production smoke

- Command: `SMOKE_BASE_URL=https://crm-ai-sdlc.vercel.app npm run test:smoke`
- CI: `.github/workflows/production-smoke.yml`
- Secrets: `E2E_ADMIN_*`, `E2E_VIEWER_*`, `PRODUCTION_SUPABASE_DB_URL`
  (missing = FAIL)

| #    | Smoke test                                | Result       | Evidence    |
| ---- | ----------------------------------------- | ------------ | ----------- |
| 1–5  | REL-003 AUTH                              | NOT EXECUTED | after merge |
| 6–7  | REL-004 CRM list                          | NOT EXECUTED | after merge |
| 8–10 | REL-006 dashboard unauth / ADMIN / VIEWER | NOT EXECUTED | after merge |

Overall: **not executed**. State after merge is **ON_MAIN — not
RELEASED.**

## Rollback

Human reverts the release merge on `main` and/or Vercel Production
rollback. Agent does not deploy or merge `main`.

## Residual risk

REL-006 residual VIEWER KPI gap is addressed on `test` by BUG-004
(stamps view); promotion is REL-007. C008 still **BLOCKED**.
