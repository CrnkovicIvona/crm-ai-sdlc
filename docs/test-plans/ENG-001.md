# Test plan: ENG-001

- Issue: ENG-001 (project foundation)
- Risk level: Low
- Impact summary: Process, documentation, CI foundation. No application.

## In scope

- Prettier format check
- commitlint configuration load
- Secret/pattern scan of the working tree
- Verify no `src/` application, no Supabase schema, no production deploy config
- Verify work is not committed on `main`

## Out of scope

- Vitest unit/integration (no application)
- Playwright e2e (no application)
- ESLint application lint (deferred)
- Vercel deploy
- Supabase migrations

## Regression pack

- None (no product behavior)

## Environments

- Local / CI

## Entry / exit criteria

- Entry: foundation files present on `feature/ENG-001-project-foundation`
- Exit: in-scope checks executed with evidence; no secrets; `main` unchanged

## Healing

Failures go to the `heal` skill; re-execution uses `execute-tests`.
