# Technical specifications

One file per work item: `<WORK-ITEM-ID>.md`.

Describe **how** approved functionality will be implemented inside
existing architecture and accepted ADRs. Do not invent product
behavior. Do not create `src/`, migrations, RLS, or endpoints in this
phase.

Unresolved technical choices are `TBD — HUMAN DECISION REQUIRED`.
Propose options only when the architecture already constrains them;
do not silently select among deferred ADRs.
