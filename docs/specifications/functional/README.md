# Functional specifications

One file per work item: `<WORK-ITEM-ID>.md`.

Describe **what** the feature must do. Do not describe source layout,
libraries, tables, RLS policies, or APIs.

Write after the business requirement (`docs/requirements/`) and before
user stories for new work. AUTH-001 received this artifact after
stories because it continued from `SPECIFIED`.
